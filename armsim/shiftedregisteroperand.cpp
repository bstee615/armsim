#include "shiftedregisteroperand.h"

ShiftedRegisterOperand::ShiftedRegisterOperand(word w, Memory *registers)
{
    // this->registers = registers;
    // rMIndex := rMIndex(w)
    // shiftType := shiftType(w)
}
